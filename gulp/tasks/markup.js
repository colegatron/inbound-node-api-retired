var gulp = require('gulp');
var config = require('../config').markup;


gulp.task('markup', function() {
 /* stop markup index.html build
  return gulp.src(config.src)
    .pipe(gulp.dest(config.dest));
    */
});
